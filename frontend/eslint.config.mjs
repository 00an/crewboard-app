// ESLint flat config (Next.js 16 default format). Wraps eslint-config-next's
// recommended rule set (Core Web Vitals + TypeScript + React Hooks + a11y).
import nextConfig from "eslint-config-next";

const config = [...nextConfig];

export default config;
